import styled from "@emotion/styled";

export const SearchContainer = styled.li`
  height: 100%;
  form {
    min-height: 100%;
    flex-direction: row;
    box-shadow: none;
    padding: 0;
    flex: 1;
    input {
      margin: 0;
      flex: 2;
    }
  }
`;
